{
  "name": "@ecommerce/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Prisma, Kafka, and metrics utilities for e-commerce services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run prisma:generate && tsc -p tsconfig.json",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^5.9.0",
    "kafkajs": "^2.2.4",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "prisma": "^5.9.0"
  }
}
