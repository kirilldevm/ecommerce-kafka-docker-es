import { SectionPlaceholder } from '@/components/common/SectionPlaceholder';

export function ProductSearchPlaceholderPage() {
  return (
    <SectionPlaceholder
      title="Find products"
      description="Search the catalog with filters (category, price, in stock) and add items to your cart."
    />
  );
}
