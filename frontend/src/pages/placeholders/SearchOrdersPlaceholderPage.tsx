import { SectionPlaceholder } from '@/components/common/SectionPlaceholder';

export function SearchOrdersPlaceholderPage() {
  return (
    <SectionPlaceholder
      title="Search orders"
      description="Elasticsearch-backed order search with debounced query, status filters, and pagination."
    />
  );
}
