import { SectionPlaceholder } from '@/components/common/SectionPlaceholder';

export function AdminProductsPlaceholderPage() {
  return (
    <SectionPlaceholder
      title="Manage products"
      description="Admin CRUD for the catalog using Formik + Yup — create, edit, and delete products."
    />
  );
}
